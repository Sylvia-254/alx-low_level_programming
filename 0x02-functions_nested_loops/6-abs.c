#include "main.h"
/**
 * _abs - fuction that computes the absolute value of int
 * @r: the value
 * Description: The standard library provides a similar funct..
 * Return: (0)
 */
int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	else
	{
		return (r * -1);
	}
}
