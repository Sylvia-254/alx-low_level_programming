#include "main.h"
/**
* mul - func that checks for checks for a digit through 0
* @a: a - Variable
* @b: - Variable
* Return: Always (0)
*/
int mul(int a, int b)
{
	
int mul;
	
	
mul = a * b;
	
return (mul);
}
