#include <stdio.h>
/**
 * main -A program that prints a line usng the printf function
 * Return: 0 (success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n")
		return (0);
}
